import { cn,useControllableValue } from '../_utils';import styles from './SegmentedControl.module.css';
export function SegmentedControl({options,value,defaultValue,onChange,ariaLabel='Mode',variant='default'}){const [currentValue,setCurrentValue]=useControllableValue(value,defaultValue||options[0]?.value,onChange);return <div className={cn(styles.root,styles[variant])} role="group" aria-label={ariaLabel}>{options.map(option=><button key={option.value} type="button" className={currentValue===option.value?styles.active:''} aria-pressed={currentValue===option.value} onClick={()=>setCurrentValue(option.value)}>{option.label}</button>)}</div>;}
