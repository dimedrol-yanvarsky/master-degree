import { Avatar } from '../Avatar';import { Button } from '../Button';import { Input } from '../Input';import { KitIcon } from '../Icon';import styles from './ChatPanel.module.css';
export function ChatPanel({variant='default'}){return <section className={[styles.root,styles[variant]].filter(Boolean).join(' ')}><div className={styles.assistant}><Avatar name="Kit" size="sm" variant="gradient"/><p>Summary is ready. Two tasks now need attention.</p></div><div className={styles.user}><p>Show only critical items.</p></div><div className={styles.compose}><Input placeholder="Message" variant="filled"/><Button iconOnly iconLeft={<KitIcon name="plus"/>} aria-label="Send"/></div></section>;}
