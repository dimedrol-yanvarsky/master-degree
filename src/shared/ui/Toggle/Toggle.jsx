import { cn } from '../_utils';import styles from './Toggle.module.css';
export function Toggle({label,disabled=false,className='',variant='default',...props}){return <label className={cn(styles.root,styles[variant],disabled&&styles.disabled,className)}><input className={styles.input} type="checkbox" disabled={disabled} {...props}/><span className={styles.track} aria-hidden="true"/>{label&&<span className={styles.label}>{label}</span>}</label>;}
