import { useEffect,useRef } from 'react';import { cn } from '../_utils';import styles from './Checkbox.module.css';
export function Checkbox({label,indeterminate=false,disabled=false,className='',variant='default',...props}){const inputRef=useRef(null);useEffect(()=>{if(inputRef.current)inputRef.current.indeterminate=indeterminate;},[indeterminate]);return <label className={cn(styles.root,styles[variant],disabled&&styles.disabled,className)}><input ref={inputRef} className={styles.input} type="checkbox" disabled={disabled} {...props}/><span className={styles.box} aria-hidden="true"/>{label&&<span className={styles.label}>{label}</span>}</label>;}
