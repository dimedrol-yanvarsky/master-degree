import { Toggle } from '../Toggle';import styles from './SettingsPanel.module.css';
export function SettingsPanel(){const rows=[['Notifications','System events and important changes'],['Security','Two-factor protection and sessions'],['Integrations','Webhook, API keys and export']];return <section className={styles.root}>{rows.map(([title,description],index)=><div key={title} className={styles.row}><div><strong>{title}</strong><span>{description}</span></div><Toggle aria-label={title} defaultChecked={index!==2}/></div>)}</section>;}
