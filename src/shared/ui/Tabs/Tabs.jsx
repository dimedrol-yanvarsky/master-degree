import { cn,useControllableValue } from '../_utils';import styles from './Tabs.module.css';
export function Tabs({tabs,value,defaultValue,onChange,ariaLabel='Tabs',variant='underline'}){const [currentValue,setCurrentValue]=useControllableValue(value,defaultValue||tabs[0]?.value,onChange);return <div className={cn(styles.root,styles[variant])} role="tablist" aria-label={ariaLabel}>{tabs.map(tab=><button key={tab.value} type="button" role="tab" className={currentValue===tab.value?styles.active:''} aria-selected={currentValue===tab.value} onClick={()=>setCurrentValue(tab.value)}>{tab.label}</button>)}</div>;}
