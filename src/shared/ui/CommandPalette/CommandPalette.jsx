import { Input } from '../Input';import { Kbd } from '../Kbd';import { KitIcon } from '../Icon';import styles from './CommandPalette.module.css';
export function CommandPalette({commands,variant='default'}){return <div className={[styles.root,styles[variant]].filter(Boolean).join(' ')} role="dialog" aria-label="Command palette"><Input iconLeft={<KitIcon name="search"/>} placeholder="Find action" variant="command"/><div className={styles.list}>{commands.map(command=><button key={command} type="button"><KitIcon name="command"/><span>{command}</span><Kbd>Cmd K</Kbd></button>)}</div></div>;}
