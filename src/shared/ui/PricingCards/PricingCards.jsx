import { Badge } from '../Badge';import { Button } from '../Button';import { cn } from '../_utils';import styles from './PricingCards.module.css';
export function PricingCards(){return <div className={styles.grid}>{[['Start','0','For prototypes'],['Team','990','For production projects'],['Scale','2490','For several teams']].map(([name,price,description],index)=><article key={name} className={cn(styles.card,index===1&&styles.featured)}><Badge tone={index===1?'accent':'neutral'}>{index===1?'Popular':'Plan'}</Badge><h3>{name}</h3><strong>{price}</strong><p>{description}</p><Button variant={index===1?'primary':'secondary'} fullWidth>Choose</Button></article>)}</div>;}
